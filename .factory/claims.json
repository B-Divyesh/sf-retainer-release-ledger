[
  {
    "id": "demo-isolation",
    "claim": "The sample demo opens in one click, is separate from your ledger, and can be reset or discarded.",
    "where": "home sample action, header Demo link, demo banner, README",
    "test": "npm run test:e2e -- --grep @claim:demo-isolation",
    "sandbox": "fresh browser context; create real data; click the home sample action; inspect sample jobs and database names; test direct demo URLs, reset, and exit"
  },
  {
    "id": "browser-privacy",
    "claim": "No account, bank connection, payment processing, custody, analytics, advertising, or online ledger copy is used; ledger data stays in this browser.",
    "where": "home facts and limitations, job dialog, receipt, privacy, terms, README",
    "test": "npm run test:e2e -- --grep @claim:browser-privacy",
    "sandbox": "fresh demo; record requests through mutation, export, receipt, privacy, and terms; require same-origin traffic and no payment or purchase control"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit.",
    "where": "home facts, README",
    "test": "npm run test:e2e -- --grep @claim:offline-reload",
    "sandbox": "fresh demo; wait for service worker; reload sample job with context offline"
  },
  {
    "id": "data-export",
    "claim": "Imports JSON backups and exports all records as JSON or CSV or one job as CSV.",
    "where": "home export section, README",
    "test": "npm run test:e2e -- --grep @claim:data-export",
    "sandbox": "fresh demo; inspect downloaded JSON, full CSV, and filtered job CSV bytes"
  },
  {
    "id": "release-status",
    "claim": "Compares payments with finished and released work to show Ready, Review, or Hold with a reason.",
    "where": "home how-it-works, job status, README",
    "test": "npm run test:e2e -- --grep @claim:release-status",
    "sandbox": "fresh demo; assert every entry type, displayed totals, and Hold after an excessive release"
  },
  {
    "id": "client-receipt",
    "claim": "Shows a client receipt that can be printed or saved as PDF.",
    "where": "job receipt, README",
    "test": "npm run test:e2e -- --grep @claim:client-receipt",
    "sandbox": "fresh demo receipt; assert configured fields and intercept the browser print action"
  },
  {
    "id": "accessible-responsive",
    "claim": "Supports keyboard use, light and dark themes, legal pages, and 390 px screens.",
    "where": "README",
    "test": "npm run test:e2e -- --grep @claim:accessible-responsive",
    "sandbox": "fresh 390x844 browser; assert focus order, touch targets, overflow, theme, legal titles, and axe"
  },
  {
    "id": "no-purchase-required",
    "claim": "No purchase is required to create jobs, use receipts, or import and export records.",
    "where": "README",
    "test": "npm run test:e2e -- --grep @claim:no-purchase-required",
    "sandbox": "fresh demo; create an additional job, open its receipt, and exercise JSON and CSV export without a license or checkout"
  }
]
